# Different analysis functions to evaluate the perfomance of a model


def loop_eval(model, dataset):
    return NotImplementedError("First we need to have the trained models!")


def to_confussion_matrix(preds, y, conf={}):
    return NotImplementedError("First we need to have the trained models!")
